type FormStyledTypes = {
  $errorEmail: boolean;
  $errorName: boolean;
  $errorPassword: boolean;
  $errorTelefone: boolean;
};

type ContainerTypes = {
  $errorPasswordId: string[];
};

export type { FormStyledTypes, ContainerTypes };
