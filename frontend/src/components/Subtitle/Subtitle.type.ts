type SubtitleTypes = {
  text: string;
  textAlignment?: 'left' | 'center' | 'right' | 'justify';
}

type SubtitleStyledTypes = {
  $textAlignment?: 'left' | 'center' | 'right' | 'justify';
}

export type { SubtitleTypes, SubtitleStyledTypes };
